const express = require('express');
const app = express();
const PORT = process.env.PORT || 3000;

app.get('/', (req, res) => {
    res.status(200).send('🚀 Anime Bot is running and healthy!');
});

function keepAlive() {
    app.listen(PORT, () => {
        console.log(`🌍 Express server is listening on port ${PORT}`);
    });
}

module.exports = keepAlive;
